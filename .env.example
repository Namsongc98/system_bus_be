# Copy to .env for local tooling that loads dotenv files. Spring Boot does not
# load this file automatically; export these variables in your shell or IDE.
JWT_SECRET=replace-with-a-base64-secret-at-least-256-bits
JWT_EXPIRATION_MS=86400000

DB_URL=jdbc:mysql://localhost:3306/quan-ly-ban-hang?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=replace-with-local-password
JPA_DDL_AUTO=update

MAIL_USERNAME=
MAIL_PASSWORD=

MINIO_URL=http://localhost:9000
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=user-avatars
